server:
  port: 8083
  servlet:
    context-path: /${spring.application.name}

spring:
  application:
    name: snnlab-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/sinanhotamis/snnlab-microservice-config
          skipSslValidation: true
